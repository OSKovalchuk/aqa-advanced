// Create a variable for the radius of the cylinder
let radius = 2;

// Create a variable for the height of the cylinder
let height = 5;

// Calculate the volume of the cylinder using the formula π * radius^2 * height
let volume = Math.PI * radius ** 2 * height;

 //Output the result to the console
 console.log("Radius:", radius.toFixed(2));
 console.log("Height:", height.toFixed(2));
 console.log("Volume of the cylinder:", volume.toFixed(2));
